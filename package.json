{
  "name": "telco-churn-platform",
  "version": "1.0.0",
  "description": "Orchestrator for Telco Churn platform",
  "scripts": {
    "install-all": "npm install --prefix backend-node && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend-node\" \"npm run dev --prefix frontend\" \"cd backend-python && .\\venv\\Scripts\\python.exe -m uvicorn main:app --reload --port 8000\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
